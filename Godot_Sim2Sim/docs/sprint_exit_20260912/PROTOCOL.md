# 行走加速转弯退出：新会话

前轮5b054b7已经封存，确认有进步但未完成用户要求。本轮继续左Shift+W更快、直立、不走歪和A/D连续转弯，只处理行走，不降低既有门槛。

有效工作上限120分钟、最后30分钟用于冻结验收、打包和清理；四核16–19、nice10、监督器有界超时。若训练，学习器必须CUDA。开发种子923000–923015，最终种子924000–924049，旧920/922最终集保持封存。基线为前轮joint-trial实际导出包；另外保留旧split包作为无回退参照。

先在新开发种子复现并最小化转弯退出失败，再依据完整命令/身体反馈分辨控制坐标、双重纠偏、转弯动量和策略响应。未复现前不启动新训练。一次只检验一个主要假设，训练或控制收益分开归因；提高速度必须同时通过直行、转弯、停止及无跌倒，普通速度保留旧包95%以上，候选各组比本包普通至少快15%。

冻结Godot/Jolt、质量、重力、碰撞、执行器、200Hz物理/50Hz决策、原始ONNX精度。Python/原生契约同步并对照；运行不依赖Python/TCP。完整开发集和额外连续/不同转向时长用例均合格才冻结；最终数据只验收，不再次选参数。最终包需真实/随机/边界数值小于1e-5、干净ARM64容器、普通九技能无新增回退、FPS/复位/暂停/机器人切换、场内长期操作、性能与资源清理。

宿主锁屏不能绕过；键盘验收仍单独记录，不能将处理器或回放测试称为OS键盘通过。

新开发基线左右转32条27过、5条退出侧移失败。最小回放为seed923000：静置1秒、加速右转5秒、加速直行2秒，侧移0.068507m；去掉初始普通直行及结尾停止仍失败，去掉初始静置变为成功，初始步态历史具有影响。原10秒截断回放重复两次，观测/动作/控制目标完全相同。

登记假设：H1同时横移和路径转向导致过度/互扰；保持actor、所有手势和速度，分别关闭路径转向前视、关闭横移输出，只变一个通道，若侧移改善则再看完整新开发集。H2路径法向反馈直接给身体横向轴且前向命令未投影，若坐标不一致重要，则一致旋转完整期望速度向量应改善。H3若通道和坐标均不能解释，检查转弯退出瞬间的动量及策略响应，不直接追加PPO。

H1结果：只保留位置反馈在标准转弯32/32通过，但最小回放侧移变为8.33cm；只保留转向则13/33。不能据此选择只针对标准回放的开关。H2一致旋转路径速度的数学不变量通过，但原生27/33，最小回放6.53cm；已撤回生产原型，代码和证据存projection_prototype。

H3进一步可检验约束：原控制在0.30前进命令上叠加0.15横向，相当于请求0.335m/s平移范数，恰在转弯恢复时额外增加负担。将横移和前进共享原请求的平移速度预算，保持横向纠偏，前向用sqrt(max(0,v_request²-v_side²))分配，零偏差时仍为原巡航速度；不改姿态/物理，不改变A/D原始转向段。这是控制可行性假设，仍以33条诊断集和完整开发集判断，不因数学成立就采用。

H3速度预算结果27/33，未修复全部诊断回放；已撤回生产开关，证据存speed_budget_prototype。

H4预登记：为S05添加残差可见的身体平面速度/高度；复用既有版本化61D状态输入，原图先屏蔽58:61，使原策略仍见零body command。比较state-visible与state-masked两个同网络、同源、同seed935101、同96更新/393216样本/CUDA预算的分支，critic完全相同。冻结原控制和物理，覆盖14秒原生转向退出课程；两分支只比较final.onnx，不挑中间最佳。若两者无稳定开发改善，不因奖励上涨或输入变多推广。训练从reset开始，评测最小回放、标准32条及完整配对操控。

H4状态可见模型117/128、零倒，最小回放侧移0.052393m但长直行仅0.199965m/s，速度回退，不晋级；同预算屏蔽对照继续完整执行。

另一个控制辨识支线：用32条已冻结左右转启动轨迹拟合一阶yaw响应，R²仅0.291，未达到预设0.7解释门槛。即使可计算临界阻尼系数，也不据此实施控制修正。证据在yaw_identification。

H6预登记：全14动作残差可能破坏已有矢状面推进节律。以e04的同源、同seed935101、同96更新/393216样本/控制/状态输入为参照，只允许两侧髋yaw/roll四维残差与探索，其他10关节目标严格保留源模型。探索仅保留四维，冻结关节仅留1e-5数值分布下限。初始四维与14维网络仍相同，不增大学习率、不改奖励、不扫参数，只比较final.onnx。此实验检验动作子空间约束，不预先声称保持关节目标就能保证物理前进速度；必须通过同一完整原生配对与95%普通速度门。

用户指出GPU利用率后调整优先级：H6仅完成原型和五项单测，未启动训练，已撤回生产改动并存lateral_basis_prototype；baseline_all也仅为待执行脚本。本轮两组CPU采样/CUDA更新均已完成，回放评测已结束，进程组110249无剩余进程。下一步优先GPU仿真与Jolt迁移诊断，不追加同类CPU采样PPO。
