Supervisor stdout/stderr is stored byte-exact in gzip files to preserve raw warning whitespace without introducing whitespace errors in Git diffs. Decompress for inspection; original .log files remain in the local session. Terminal events and process ownership are also in active_budget.json and closeout/resources.json. A completed supervisor process may have a nonzero child return code; consult job_results rather than treating directory existence as success.
