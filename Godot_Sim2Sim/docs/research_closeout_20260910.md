# 2026-09-10 研究提交收尾

实验基线及九模型选择保持原样，本次补齐新检出目录的准备和运行入口。
模型结论仍以 [实验报告](../RESEARCH_RESULT_20260910.md) 为准；未宣称全面达到 MuJoCo 品质。

- 新增 `research.setup`：在指定目录快照外部权重、创建独立时间预算、生成场景、采集 anchor 所需 MuJoCo 观测。
- 研究目录可由 `SIM2SIM_RESEARCH_DIR` 指定；已有实验拒绝重新初始化，旧默认目录兼容保留。
- 新增 `research.bundle`：搬移模型包后根据已核验的本地文件定位九个模型，避免历史绝对路径和默认模型回退。
- 完整 A/B 支持显式新输出目录，并在启动物理测试前检查比较模型是否齐全。
- 测试改用环境配置的资源目录，移除开发者机器的绝对路径回退。
- README、SIM2SIM、HANDOFF 保留旧交接内容，并增加当前实验报告与复现入口。
- Git 记录候选包和 18 个原版／旧模型对照包的清单与 SHA-256；两份 ZIP 独立交付。

操作见 [REPRODUCING.md](../REPRODUCING.md)，模型清单见
[artifacts/research_20260910.json](../artifacts/research_20260910.json)。没有登记不存在的远端下载地址。
完整训练 checkpoint 和逐回合数组仍保留在本地实验归档；初始化新实验不等于重建全部历史训练。

## 验证

验证使用从 Git 导出的隔离目录，重新生成四套场景、快照 18 个外部 ONNX。
并行任务的视觉提交 `924adf7` 在最终验证中也已包含，本收尾提交不重复修改其内容。

- 最终 unittest：**195 项通过，无跳过或失败**。第一次最小资源运行有两项历史 checkpoint 测试跳过，补齐显式资源后最终全部执行。
- residual 冒烟：两次迭代，覆盖 critic 预热、保存、导出和实际评测。
- anchor 冒烟：先采集 MuJoCo 轨迹，再运行两次迭代，确认 actor 实际更新和导出一致性。
- 搬移后的九模型包：哈希、manifest、维度和有限推理校验通过；右踢 6/6，连续操作四项动作均通过，无普通动作跌倒。
- 完整 A/B 冒烟：29 组、158 个有效物理回合、1 条连续流程和 6 条按键序列均完成，引擎错误为零。任务本身的失败照常记录，不把这组运行视为新的质量验收。
- 三阶段封存文件分别 272、24、24 项哈希全部一致；原候选 ZIP 的 SHA-256 保持不变。

结构化记录与被测源文件哈希见 [research_closeout_validation.json](research_closeout_validation.json)。
详细本地日志保留在 `results/git_closeout_20260910/`。
