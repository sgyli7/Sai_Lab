# 2026-09-11 八小时执行记录

开工：北京时间 01:09:15（UTC 2026-09-10 17:09:15）。
分支 `codex/overnight-20260911`，基点 `da5022170a6da02260826627d574a0c56a18f6c7`。
有效工时以 `results/research_20260911/active_budget.json` 为准；后台工作计时、并行取并集、未确认的断线空档不计。限额 28,800 秒，23,400 秒后停止新增候选，最后 5,400 秒用于验收和收尾。

最新验收结果见 [RESULT.md](RESULT.md)。以下按时间保留开发过程，当时的“待完成”状态不覆盖最终记录。

## 最终收尾索引

- 候选在累计约 386.66 分钟完成选择。完整开发主动制动 49/56、零跌倒，按预先记录的平局规则选出近停速度反馈与起始姿态修正的组合；其他八模型不变。
- 固定 41/56 控制器去掉前向速度输入后为 6/56，支持该控制器的速度反馈作用，不扩大为对所有优化算法的结论。九项首轮训练均有单独对照和保留旧版理由。
- 最终参考包与候选各完成 690 条干净容器回放。主动制动 0/210→180/210，跌倒回放 183→0；30 条仍停稳过迟，其中 4 条还有持续倒滑。基础行走 13/30→28/30，七组行走无旧成功丢失、速度至少保留 99.91%。详细质量缺口见最终报告。
- 九模型同 SHA 的真实观测、随机和边界输入自检通过。完整九技能 30/144 FPS 回放逐项动作、观测、上一动作、命令和目标完全相同。实际分发压缩包完成干净环境解包、完整校验和自检。
- 最终图形长测完成 2,700 仿真秒、135,000 决策、78 次释放重载、39 次显式复位，正常退出；速度 0.993 倍实时，峰值 RSS 751.7 MiB。生命周期回放的复位不参与质量评分。
- 宿主窗口定向输入通过，桌面始终锁屏，真实全局键盘验收仍未完成。没有绕过锁屏；最终验收结果没有反馈给本轮参数搜索。
- 证据包包含 11,018 个已逐文件复核的证据文件，另加自身清单，约 1.79 GB；20 个完整 checkpoint 均可反序列化、状态有限且原 ONNX 锚点 SHA 匹配。重复工程缓存与多数开发原始轨迹保留在本地。最终工时和本地提交号由交付工作回执记录。

以上是最终结果摘要；下面的时间段记录保留当时上下文，不把历史阶段性通过改写成全项验收成功。

## 固定协议

- 平地单机器人、九技能 Linux ARM64 独立程序，不依赖 Python/TCP。
- 现有 Jolt、质量、重力、碰撞、执行器参数不变；200 Hz 物理、50 Hz 控制。
- ORT 1.29.0 CPU，保留原 ONNX 和内部 DOUBLE 计算；动作误差必须小于 `1e-5`。
- 旧模型固定在 `results/research_20260911/incumbent_models`，来自前次 `delivery_v3/models`；文件校验在本轮 `protocol.json`。
- 开发种子 915000–915007，最终保留种子 916000–916029，不用于调参。
- 轮滑 S 制动：2 秒内进入停稳状态（0.2 秒平均速度 <0.05 m/s 连续 1 秒及站稳）；距离 ≤max(0.15 m, 初速×1.2 s)，无跌倒、持续倒滑、自动复位、冻结或削弱巡航来达标。
- 行走配对平均速度至少保留旧模型的 95%；九项分别实验并记录去留，不按训练奖励直接替换。
- 最终冻结后一次性保留种子评测，不继续按最终结果选参数。九技能独立包和制动同时通过才算成功。

## 已有证据（开发阶段，非最终验收）

- 原生 ORT 扩展已编译，九模型共 2,304 个随机/时间输入通过数值对照，错误输入拒绝和重复加载释放在编辑器运行路径通过。
- Native PlayBrain：4,800 周期状态机对照通过，1,024 组坐标/观测/控制/放球输入通过。相反方向键在技能结束时的无顺序默认选择差异已修正。
- 共享物理改动只抽出控制器启动、积分后的传感刷新、PD/积分计数三个接口；60 项原有相关测试通过。
- 原生与 Python 完整闭环 W→S：500 周期一致，最大观测/动作误差约 `5e-15`；两边在 t=4.8 s 跌倒（样本记的是积分前时间；规划探针记录积分后时间为 4.78 s）。原生部署没有修复原有制动缺陷。
- 步行连续技能 46 秒、2,300 周期的完整闭环对照也一致；其中前滚姿态超过通用倾倒阈值属于需按技能评分的事件，不能直接当作意外跌倒。
- 步行→轮滑→步行、2 次复位：900 周期影子对照通过，未跌倒。
- 首个导出包可在仓库外和禁网、无 Python 的 Ubuntu 24.04.4 ARM64 容器中加载并运行，初次退出发生 invalid free。调试器定位为 `godot::MethodBind` 数组释放的调试/发布内存布局不匹配；分开构建 `template_debug` 与 `template_release` 并按 feature tag 加载后，禁网容器运行/退出与发布版机器人切换通过。未跳过 ORT Session 释放。完整验收尚未结束。

## 控制版本

迁移初版保留原控制契约，闭环轨迹完全一致。新增显式“站立 7”入口，与行走模型 `use_stand_policy=false` 的闲置逻辑独立；Python PlayBrain 和原生端同步增加 `stand_hold`，未按 7 时保持原行为。独立程序用暂停按钮替代训练用时间倍率滑条，以保持物理步长不变。

## 当前依赖

- Godot 与模板：4.7.2 stable `ed1daf0bf`，官方 ARM64 模板。
- godot-cpp：`godot-4.5-stable`，提交 `e83fd0904c13356ed1d4c3d09f8bb9132bdc6b77`。当时官方标签未提供 4.7；前向兼容仍须同时验证编辑器与 release 模板。
- ORT：官方 `onnxruntime-linux-aarch64-1.29.0.tgz`，SHA-256 `e1799098ebc054b370f6176a450f158720f297818c613e5dc99b92e2ec82346f`。
- Godot 模板压缩包 SHA-256 `f298490b8d44d934be425a5a65a51bf15f422428b229a06a6e11d9ffea248011`。
- Ubuntu 24.04 镜像 digest `sha256:224a1869083a311ef3f13648a154ba79832fbef6364d31493642ca03082da254`。

原生库由 `.gdextension` 依赖项导出；模型通过 Godot FileAccess 从 PCK 读取字节。导出使用独立 feature tag 覆盖入口，保留训练入口。[Godot feature tags](https://docs.godotengine.org/en/stable/tutorials/export/feature_tags.html)、[GDExtension 依赖导出](https://docs.godotengine.org/en/stable/tutorials/scripting/gdextension/gdextension_file.html)。

## 后续必做

长时运行/反复释放和最终窗口验收；九技能各一轮实验，优先定位制动再训练；最终保留种子、干净环境及窗口验收；包、校验清单、原始轨迹、工时和本地 Git 提交。不得在这些工作完成前宣称本轮成功。

用户已取消额度处理，由别的任务负责。本任务不得查询或重置额度。

## 有效时间约 80 分钟补记

- 离线 Ubuntu 24.04.4 ARM64 中，发布包九模型共 2,880 条输入（每项 256 合成 + 64 真实观测）通过，最大动作误差 1.42e-14；包校验和通过。容器只挂载导出目录、禁网、没有 Python 或源码仓库。
- OS 窗口事件验证覆盖显式站立、步行、轮滑、下蹲、切换、复位、暂停恢复。XTest 注入未产生窗口事件，失败证据保留；后用仅投递给本次 PID 窗口的 XSendEvent 验证 Godot 键盘输入路径。暂停 2.08 秒前后物理步数和 episode 时间相同。
- 固定回放在渲染上限 30 / 144 FPS 下的 400 次动作完全一致；实际墙钟速度另由导出程序性能测试记录。
- 有效工时监督、原子训练检查点及有界重试队列已实现。真实 SIGTERM 后保存完整更新；新进程从 latest.pt 恢复并开启新物理回合。两项仅是基础设施测试，没有计作技能优化实验。
- 17 条原生名义场景全部运行并成功评分。站立、行走、坐起、捡地、左右踢、前滚、轮滑蹲起达到原有任务成功判据；行走末端偏航 26.1 度，左右踢各约 28.6 / 24.1 度，仍有质量改进空间。
- 轮滑原始 3 秒推进→S，在制动后 0.80 秒倾倒；初速 0.685 m/s。多种推进、转向及重复刹车仍失败。释放 / 空格不会跌倒，但未满足本轮停稳门槛。没有以倒地后的低速作为制动成功。
- 独立离线评分器复用原研究 World.measure / summarize；捡地的 Python 与原生闭环所有任务指标一致。七项制动评分反例测试通过。

## 有效时间约 108 分钟：制动试验与训练基础设施修复

- 新增可复现的控制单因素试验：减速度 5 档、负向制动力 6 档、制动脉冲 7 档、目标航向保持 2 档，各覆盖 7 项主动刹车及释放/空格对照。全部完成，但均为 0/7 主动制动通过，全部保留旧版。减弱到 -0.1 或短脉冲 0.15 秒可避免名义用例跌倒，却不满足时限/距离/倒滑约束。
- 控制参数、代码和模型校验值随实验保存。新增 Python / 原生同构 MotionControl；实际脉冲及减速回放各 500 步的影子对照通过。未改变正式部署默认命令。每个新回放套件现在保存完整 Godot 运行快照，后续源码编辑不会混入正在运行的套件。
- 开发种子 915000–915007 共 136 回合基线：站立/坐起/捡地/左右踢/轮滑蹲起均 8/8；前滚 7/8；步行 3/8（均站稳，主要是松键方向漂移）；所有主动制动均不合格。
- 发布包持续 1,800 仿真秒 / 90,000 决策，8.54 倍实时，推理 P50/P95/P99 为 249/411/512 微秒；稳态 RSS 182,636–183,380 KiB。另完成 40 次机器人切换/ORT 重新加载，退出正常。此为开发性能数据，最终冻结后再正式记录。
- HUD 调整为两行技能按钮，避免按钮与方向控件重叠；状态中文化。第二轮宿主窗口键盘事件验证及 748 步 Python 影子对照通过。
- 首个制动残差实验在第 7 次 PPO 更新出现非有限价值损失，完整保留失败与第 6 轮检查点。两次确定性重放复现并定位：侧倾 77.76 度时，旧测量代码按当前躯干侧向位置重新划分轮组，把四轮归为同一侧，另一侧 foot_pos 为空，terminal critic obs[66] 为 NaN；0 × NaN 污染回报。
- 改为按 ankle_l_v1 / ankle_r_v1 固定关节祖先归属轮组。未修改物理参数、模型观测或执行器。评分版本记录为 physical_tasks_v8 / roller_throttle_heading_v2，旧证据原样保留。实际失败帧加入回归夹具，修复后完整跑过 12 次更新及导出数值检查；原实验从第 6 轮完整检查点、全新物理回合恢复。
- 制动残差只在负向油门开启；单测证实即使残差权重变化，正向推进与中性命令的导出动作仍与工厂图逐位相同。新增训练课程使用真实键盘推进前缀，保留物理状态和上一动作后进入制动。该结构是否改善刹车仍待开发评测，不能视为已达标。

## 有效时间约 173 分钟：恢复、控制对照与制动姿态搜索

- 上一轮制动训练已在后台正常完成；之后约 6 小时 37 分钟完全停工的区间没有计入工时。恢复时核对进程、完成标记和账本，没有重复启动已完成任务。预算测试 10 项和实验队列测试 7 项再次通过。
- 原制动 PPO 最终模型完成 72 条原生开发回放（56 条主动制动，16 条释放/空格对照）：主动制动 0/56 通过，16 条跌倒。原始 W3→S 的八个种子均不再跌倒，但末端仍有约 0.63–0.66 m/s 速度，明确拒绝替换。模型奖励上涨不作为改善证据。
- 行走航向反馈 gain=1 的基础直行八种子从 3/8 提升到 8/8，平均速度保留 100.3%。扩大到长直行、转弯、倒退、横移、反复起停后发现一个旧版成功用例退步；gain=1.5/2/3，以及小幅平移命令校准 1.05/1.1/1.2，仍有配对退步，均暂不部署。评分保留 requested_command，不能因反馈修正把用户松键误认为在主动操作。
- 增加 25 分钟行走残差实验：原模型为精确锚点，强化速度跟踪，覆盖实际长按键、混合操作、后退/横移/转向；终局 checkpoint 预先指定为 final.onnx，训练完成后在 56 条原生开发用例评测。
- 其余七项各 6 分钟训练及八种子原生对照顺序执行。已完成的站立、坐起、捡地、左踢均保留旧版：站立丢失一例成功；坐起动作变化很小但位移/偏航变差；捡地深度只改善 0.15 mm，位移与动作率变差；左踢身体偏转增大。每项保留 comparison.json 和独立 decision.json，尚未把实验完成误计为改善。
- 新增限幅、仅负油门生效的五参数对称关节姿态搜索，正向/中性指令保留原 ONNX 图。零改动两种子与原生完整闭环动作误差约 5e-15，跌倒时刻、制动距离一致。原图 DOUBLE 内部运算和非制动动作逐位保留有专门导出测试。
- 第一轮搜索缺少站姿质量的连续引导，偏好低蹲停下，被硬判据拒绝；保留最后完整代次后主动中止。第二轮加入身高/倾角引导，并改用每个试验全新 Jolt 进程，避免重复 reset 的求解器历史影响相同种子的制动前速度。最终均低速无跌倒，但约 16° 前倾未满足站稳。
- 第三轮局部搜索在一个开发种子达到 1.84 秒内确认停稳、距离 0.243 m、无跌倒/持续倒滑；另一种子仍失败。正在执行全部 72 条原生开发回放，未采用该模型。刹车后切中性脉冲的组合对照反而出现倒滑，拒绝使用。另启动从该制动姿态模型出发的小残差训练，目标改善站稳恢复，并加重制动收益抑制持续滑行。
- 原生异常路径：空/损坏模型、输入输出维度、外部类型、NaN/Inf 输入、非有限动作、失败加载后旧模型失效、释放后恢复，开发版与发布版均通过。发布版使用与候选包相同二进制/库的测试 PCK。新建最小测试工程触发固定 Godot 编辑器首次导入/退出崩溃，失败与 GDB 记录保留；测试器改用现有完整工程的导入缓存后导出及运行成功。此测试工程故障不计作应用验收成功。
- 自检夹具增加显式命令边界、时间 0/1 邻域及朝向 ±π 邻域。最终打包前还须重新生成含真实轨迹的九模型夹具并在干净环境核验。

保留种子 916000–916029 仍未使用；正式模型未替换；最终包、连续技能演示、最终验收和本地 Git 提交尚未完成。


## 有效时间约 219 分钟：九项实验完成，继续解决制动

- 九项均已完成至少一轮模型训练和独立原生开发对照。其余七项最终全部保留旧模型：右踢和前滚的平均航向仅改善约半度、配对结果混合；轮滑蹲起没有明确改善。行走残差虽改善基础直行，但丢失长直行种子 915004 的旧版成功，拒绝替换。
- 行走控制改为“发生过移动、松键后等待 0.5 秒，再保持稳定下来的朝向”。gain=6 的 56 条开发回放没有丢失旧成功，基础直行 3/8→6/8；七组松键偏航均值均改善，移动速度至少保留 100%。基础直行松键位移增加约 1.7 mm，反复起停位移与动作率改善。作为待最终冻结的控制候选，未修改部署默认值。
- 多场景制动姿态加俯仰反馈搜索，原生 72 回放均无跌倒，但主动制动仅 9/56 通过：1 秒推进 1/8、3 秒 3/8、转弯后 5/8，其余全失败。未达到硬门槛，保留为训练起点，不作为交付模型。
- 并行训练进程发生临时端口争用，已定位到 Godot listen 失败。启动器增加跨进程互斥、每次唯一日志、确认本子进程监听后才连接，hello 失败立即清理。占用端口回归确认不会连接到外来监听者；16 个并行真实启动与新增两项启动测试通过。失败搜索没有完整代次，记录保留；修复后仅显式重试一次，正常完成。
- app-server 重启后核对全部进程、最后 checkpoint 和 completed.json；后台搜索已正常结束，没有重复启动。该次 root 心跳缺口中约 645 秒由监督任务覆盖，末尾约 210 秒完全停工，未计费。累计工作仍以区间并集为准。
- 新假设：原 61D 策略观测包含关节、重力、角速度、上次动作和命令，但没有平移速度及身体高度；尝试只让新增残差读取前向/侧向 COM 速度及躯干高度。新契约 `planar_com_velocity_height_v1` 明确占用 obs[58:61]，ONNX 输入屏蔽节点让原锚点仍读取三个零。原图节点和 DOUBLE 张量不变；旧模型及旧契约不变。
- Python 训练/Play/离线对照和原生引擎同步接入有声明的状态契约，未知/不匹配声明直接失败。零残差的四个原生完整闭环与旧锚点动作逐位相同，Python 影子对照通过；合成双精度图保留及速度坐标转换单测通过。正在运行两组配对 25 分钟实验：同一制动姿态起点、种子、奖励、残差规模和预算，仅状态可见性不同。
- 已确认宿主 GNOME 锁屏会阻止全局键盘注入，已请求用户解锁；不尝试绕过锁屏。窗口定向事件的通过记录不能替代真实宿主全局键盘验收。

九技能独立包和制动同时通过的完成条件保持不变；最终 30 个保留种子仍未使用。


## 有效时间约 238 分钟：接口回归与组合操作

- 状态契约、PolicyBundle、ONNX 导出、制动残差、物理任务语义及记忆契约共 54 项回归检查通过。
- 新观测契约的零残差模型与行走候选控制组合运行 68 秒，实际调用全部九技能、两次切换机器人、一次显式复位；3,400 个决策的 Python 影子核对通过，最大观测误差 4.62e-14、动作误差 5.11e-15。前滚期间触发通用倾角阈值，不据此作九技能质量通过声明。
- 组合测试第一次将站立 tap 写成 stand_hold，完整性断言检测到实际只调用八技能；失败轨迹保留，修正为 stand 后重跑通过。原生回放入口增加未知命令、无效时长及乱序片段的显式拒绝。真实错误回放验证返回码 2，未静默接受。
- 八组制动动作限幅对照（工厂／姿态 v4 × 四种限幅），每组六例均未通过停稳门槛，全部拒绝。限幅只改变控制输出，没有改变关节范围或电机参数。
- 18 组渐进绝对关节姿态对照均失败。这一组把位置目标限制在关节物理行程内，可能削弱原策略通过位置误差维持的支撑力；不能据此断言旧模型目标越界就是错误。随后单独比较从成功稳定片段提取的原始控制目标，保留冻结的驱动方式。
- 两组 25 分钟 PPO 配对实验仍在运行。该对照比较整个状态输入契约：actor 新增可见状态，critic 的前 61 维也变化，但 critic 原有额外输入已包含速度和高度。因此不将其解释成严格的 actor-only 消融；这一限制在最终评测前记录于 ablation_scope.json。
- 补齐 STANDALONE.md 的发布使用、构建、回放、计时恢复和实验观测契约说明。尚未冻结最终模型或使用最终保留种子。


## 有效时间约 280 分钟：配对结果与干净环境回归

- 状态可见与不可见的两组 25 分钟 PPO 均已完成。状态可见候选 56 条主动制动无跌倒但 0/56 通过；不可见候选同为 0/56，出现 6 次跌倒。前者稳定性较好，但仍倒滑、停稳过迟或转向漂移，因此都保留旧模型。
- 把刹车速度奖励方差从 0.09 缩小到 0.0025 的下一轮训练，原生主动制动仍 0/56，通过站姿收益没有转化为合格停止，且出现 22 次跌倒，明确拒绝。后续单独检验连续速度惩罚，保持原有奖励参数，避免把“更严格”当作必然更有效。
- 原始控制目标的局部搜索完成四代，最佳八例无跌倒但零通过。根据末态线性响应拟合提出的三组站姿修正全部在过渡中跌倒；最终站姿拟合不能替代完整动力学验证。速度条件的双策略混合最佳 3/8，与其姿态锚点相同，没有通过率改善。
- 行走全程朝向纠正与前进命令放大组合六组全部存在回退，其中部分平均速度低于旧版 95%。继续保留已通过 56 回合开发对照的“松键后 0.5 秒、gain=6”控制候选。
- 从冻结工程导出的 integration-v3 开发包，在只挂载包、禁网、无 Python/仓库的 Ubuntu ARM64 中，九模型随机、边界、真实输入自检通过；17 条开发回放全部运行并完成评分，错误数为零。这证明独立部署路径可用，不能解释为制动质量通过。
- 套件 schema 2 保存并核对完整运行快照（控制脚本、配置、模型、原生二进制）；恢复时拒绝混入改变的控制或库。17 条干净环境回放恢复全部复用完整记录，没有新增尝试。单元回归同时覆盖模型不变但控制/库改变的情况。
- 新刹车航向控制的 550 决策原生/Python 影子对照通过，最大动作误差 5.11e-15。该控制没有提高通过率，作为失败实验保留，不部署。
- 回放入口增加空对象、错误数值和非有限/负时长拒绝；第一次发现 Godot 把字符串 nan 转成零，造成无限运行，超时证据保留后改为转换前验证数字格式。该修复不改变合法控制或物理行为。

新启动的速度相关五关节残差搜索使用同一个状态契约，正向/中性输出保留原 ONNX 图。最终保留种子仍未使用，最后 90 分钟继续保留。


## 有效时间约 295 分钟：原始制动回放首次合格

- 速度相关关节残差搜索完成四代，导出数值误差为零，正向/中性输出逐位保留。全部 72 条原生开发回放无跌倒，56 条主动制动中 17 条通过：1 秒推进 0/8、2 秒 4/8、3 秒 6/8、5 秒 3/8、转弯 3/8、重复刹停 1/8、蹲起后制动 0/8。相较此前姿态锚点 9/56 提高，但也丢失六个该锚点的成功配对，尚不部署。
- 原始无随机扰动回放（静置 1 秒、W 3 秒、S）首次满足本轮门槛：初速 0.685246 m/s，1.72 秒确认停稳，制动距离 0.223782 m，无跌倒、持续倒滑或复位。证据位于 brake_velocity_pose_nominal_v1。1 秒推进及蹲起后制动仍失败，不能据此称本轮完成。
- 下一轮直接通过完整原生程序搜索，包含真实下蹲策略切换和连续三次刹停；旧单 actor 命令带不能代表这些技能过渡。所有候选使用实验开始时冻结的引擎工程，开发种子仍为 915000/915001，验收阈值不变。
- 开发包 runtime-v4 修正发布日志缓冲，真实生命周期可用于稳态内存采样。短时验证已出现有效稳态样本；此时训练仍在运行，数值仅用于验证测量工具，不作为正式性能。
- 使用同一导出程序的 Godot Movie Writer 完成 8 秒、480 帧录制，保留 AVI、MP4、400 决策轨迹。Python 影子对照通过，图像检查确认 HUD 和机器人可见；录制不等同于真实键盘验收。
- 干净容器套件现在先实测 Ubuntu 24.04、aarch64、无 Python、无仓库挂载及无网络路由，保存独立环境记录。一次调用误写用例文件名，失败保留；正确用例随后通过。非法回放在实际 release 包中返回 2，未静默切换为交互运行。

## 有效时间约 338 分钟：完整原生搜索与发布完整性

- 完整原生速度残差搜索的第 2 代候选通过 30/56 条主动制动；无扰动原始 W3→S 用例在 1.70 秒确认停稳、距离 0.220457 m，无跌倒。该搜索最终候选通过 38/56，全部 72 回放无跌倒，但重复刹停只有 2/8，不能按筛选种子的 10/14 宣称泛化通过。
- 加入五项直接俯仰反馈的候选在两个筛选种子达到 12/14；扩大到八个开发种子为 41/56、零跌倒。分项为：1 秒推进 0/8、2 秒 7/8、3 秒 7/8、5 秒 8/8、蹲起后 7/8、重复刹停 5/8、转弯后 7/8。仍不替换正式模型。后续分别检查角速度阻尼、四种子细化，以及只作用于低速高身位起始状态的局部修正。
- 连续速度惩罚 PPO 完成 316 次更新、645,720 样本；原生 1/56 通过、无跌倒，拒绝替换。只对大于 15° 前倾补偿的局部搜索仍为 9/14，未建立优于完整俯仰候选的证据。所有实验保留原物理参数和非制动原图。
- 发布包 runtime-v5 内置原始 Noto Sans CJK 字体及许可，避免依赖目标系统字体。模型与字体启动时均核对 SHA，中文必需字形也实际检查；没有系统字体、没有 Python、禁网的 Ubuntu ARM64 容器自检通过。真实导出程序的 8 秒录制已检查中文 HUD 显示。首次录制命令使用了不存在的用例路径，未启动程序，修正后成功。
- 完整单元与集成测试共 230 项，228 项通过、2 项跳过（full_unit_suite_v1.log）；字体加入运行快照校验后另行通过针对性恢复测试。C++ debug/release 扩展增量构建通过。
- 当前旧九模型的 68 秒连续操作回放调用全部九技能、两次换机器人、一次显式复位。3,400 决策影子对照最大动作误差 5.11e-15，完整观测误差 4.62e-14；所有技能片段结束时均未失衡，前滚期间的通用倾角触发不能误算为结束跌倒。此为集成检查，不替代逐技能成功判据。
- 新夹具只接收与对应技能模型 SHA 相同的真实轨迹，明确拒绝把旧/新轮滑契约混作同一模型证据。九项各 64 条真实观测，加随机与边界输入的原生自检通过；不匹配的轮滑轨迹确实被排除。

最终 916000–916029 保留种子尚未使用，正式模型未替换。390 分钟后停止增加优化候选，最后 90 分钟用于冻结、独立验收、打包、报告和本地提交。宿主仍锁屏，真实全局键盘验收待解锁。
