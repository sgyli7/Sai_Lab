# 联合行走/加速续轮

前一轮已提交 `847513e`，作为有明确缺口的基线；本轮是用户持续目标的独立会话，不重开已封存的最终测试。用户允许自主确定细节，目标仍是左 Shift+W 更快、不倒、不走歪、兼容 A/D；只处理行走。

会话 `results/sprint_joint_20260912/`，有效工作上限 120 分钟、最后 30 分钟保留验收与清理，四核 16–19、nice 10、监督器和有界超时；PPO 学习使用 CUDA。开发种子 **921000–921007**，新最终种子 **922000–922029**；训练 seed 931101 起。旧 920000–920029 不再调参。

## 已确认的诊断更正

旧 1800 秒单向回放在 222.48 秒跌落，身体 x=20.079、倾斜仅 3.67°，双足已越过场景地板边缘。冻结 `main.tscn` 地板为 40×40 米。四秒最小对照仅改变初始水平位置：中央不倒、x=19.75 起步在 2.48 秒跌落，身体 x=20.085、倾斜 7.78°。没有改地板或物理参数。

因此该长回放只能说明离开有限地面，不能归因为长期平地策略不稳。原始日志保留，旧最终集的 34 个方向/停止失败仍有效。后续长测先登记转向/停止路线，并核验机器人持续位于场地内；越界和场内跌倒分别报告，越界不能作为有效平地长测通过。

## 假设和对照

1. 冻结普通 actor 与 sprint 的切换限制了退出质量。先把 S05 同时用于普通/加速槽，仅由显式不同速度命令区分；原生按真实流程评测，保留完全未训练的结构基线。
2. 联合继续训练可以同时修复普通前缀和加速退出。使用相同 61D/14D 来源 S05、部署反馈、真实前缀库，普通阶段也实际由学习 actor 执行并学习。训练完整普通速度和加减速限制，避免仅同步转向上限造成新契约差异。先固定 96 轮，依据原生质量决定去留。
3. 若联合短课程仍有重复操作问题，再单独对照更长的连续课程；不要同时修改物理、奖励、模型输入。新的结构/训练配置必须单列，不能把未配对的收益归因于单因素。

初步未训练联合 actor 用普通请求 0.22 得到 11/16、0 倒，但实际普通长直行约 0.148 m/s，比旧约 0.167 慢，拒绝该命令标定。正式联合训练先固定普通请求 **0.25**、加速 **0.30**、两者转向 **0.8**，路径 P=4、朝向 P=6/限幅0.6。这是改变普通控制的候选，不能假称普通模型不变；另外八技能及工作区默认模型不覆盖。

## 去留标准

沿用旧 `walking_sprint_v1` 全部方向、侧移、停止和无跌倒硬门，以及候选对同包普通 W 平均速度至少 +15%。另与旧 S05 包、旧普通模型在相同种子和手势配对，普通行走实际均速至少保留 95%，不得通过降低普通速度伪造加速收益。

完整八场景开发集首先全部满足硬门，再验证普通行走其他方向、八技能/复位/暂停/换机器人、长时间场内连续操作、数值 `<1e-5` 和 ARM64 独立运行。只有整体更好且无已知回退才晋级；未达标就保留原默认版本。最终候选冻结后一次性测试新保留集，不能按最终结果继续选参数。场地、Jolt、质量/重力/碰撞/执行器、200 Hz 物理与50 Hz 决策均冻结。

附加已登记控制假设：未训练联合基线 47/64 的失败事件为 15 个直线侧移、2 个停止，转向均通过。`walk_path_lookahead=0.3 m` 令直行目标朝向为“原路径朝向 − atan2(横向误差, 0.3)”，通过原有转向输入帮助横移纠偏；只有直行生效，A/D 原请求和物理保持。同一 S05、普通0.25/加速0.30、完整8开发种子，与不开启时逐项对照；Python/原生同步、默认关闭，收益计入控制改动。

控制对照结果：同一未训练联合 S05，开启0.3m前视后由47/64升为62/64、零倒，停止全过；仅剩转弯后两条侧移0.05047/0.05734m，朝向误差6.67/8.29度。下一项固定0.2m前视，缩短0.22m/s时的纠偏时间尺度，仍完整8开发种子核查15度朝向、速度和停止，不能只重测失败两条。

联合继续训练 j02 为59/64、零倒，但长直行降到0.1833m/s，拒绝。最后增加一组64轮×4环境×1024步的教师约束对照，seed931102、源S05、前视0.2、其余相同：成功轨迹重放 KL 与当前占用状态 KL，同为权重0.02、每批384条、冻结探索std0.035、critic预热8轮。参考数据只来自新训练种子931201–931204，63个完整成功回合抽得13033行真实观测，不使用开发或最终种子。固定同一原生64条开发集验收最终ONNX，不按训练奖励或中间checkpoint挑选。

更短前视0.15/0.10分别59/64、56/64，否定继续缩短。0.2前视唯一失败的右转退出轨迹显示，偏移期间横移命令反复卡在+0.15、转向命令卡在+0.6；身体横摆峰值越过5cm。单独对照横移策略命令上限0.20（源训练范围内），其他参数和S05权重不变，以完整64条验证是否属于纠偏命令权限不足，不改执行器物理限矩。

针对性课程候选：`nativeleft`/`nativeright`直接复用独立程序14秒输入模板，明确包含2秒直行、5秒加速转弯、2秒继续加速直行及4秒停止。训练显式14秒episode，其他六种课程保留，只有该课程候选改变时域；恢复必须使用同一时域。700行完整原生轨迹与训练观测/命令/动作一致，最大5.33e-15。若当前教师对照仍未过关，j05沿用j03源/种子/教师/64轮预算，只更换完整输入课程与所需时域，不改物理或评分。

j05实际原生结果仍63/64，右转退出一条侧移5.252cm，长直行0.2153m/s，未胜过未训练控制基线。补一项控制假设：现有路径反馈只按位置纠偏，对横向步态动量反应滞后。启用已存在、默认关闭的横向速度阻尼0.5（P=4，对应0.125s先导时间，速度用已有0.8/0.2滤波），S05/前视0.20/其他命令全部保持。完整64开发案例验证，不改物理、不选中途模型、不使用最终种子。若无全项改善，则结束新增候选并冻结最好的已测预览，保留失败。
